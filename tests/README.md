# Cross-Layer Tests

This area contains contract fixtures and integration guidance for the native parser and FastAPI service. Application unit tests remain beside the TypeScript server code for the managed web runtime.
